# dependencies
node_modules/
.pnp
.pnp.js

# builds
dist/
build/
out/
*.tsbuildinfo

# next.js (next-env.d.ts is committed so a fresh `npm run typecheck` is green)
apps/**/.next/

# testing
coverage/
test-results/
playwright-report/
blob-report/
.playwright/

# env — never commit secrets
.env
.env.*
!.env.example

# data stores / uploads (runtime)
*.sqlite
*.sqlite3
*.db
/data/
/uploads/
/artifacts/
**/artifacts-out/

# logs
*.log
npm-debug.log*
logs/

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# caches
.turbo/
.cache/
.vitest/
.venv/
**/__pycache__/
.pytest_cache/
*.pyc

# foundry
packages/contracts/cache/
packages/contracts/solidity/out/

# operator-only persona seal salts (guardrail #3 — never public)
apps/web/.persona-salts.json

# private submission-day capture bucket (screenshots, receipts, operator notes)
/evidence/

# Per-run e2e stack coordinates (written by e2e/global-setup.ts).
e2e/.stack-state.json
