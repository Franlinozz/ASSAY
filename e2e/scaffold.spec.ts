import { test, expect } from '@playwright/test'

// Scaffold placeholder — real browser-driven e2e specs (both themes, desktop + mobile)
// land in the frontend phase. This test uses no browser fixture, so `test:e2e` stays
// green without installed browsers until then.
test('scaffold: e2e harness is wired', () => {
  expect(true).toBe(true)
})
